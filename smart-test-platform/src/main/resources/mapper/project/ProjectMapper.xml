<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.smarttestplatform.module.project.mapper.ProjectMapper">

    <resultMap id="BaseResultMap" type="com.example.smarttestplatform.module.project.entity.Project">
        <id property="id" column="id"/>
        <result property="projectName" column="project_name"/>
        <result property="projectKey" column="project_key"/>
        <result property="description" column="description"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
        <result property="status" column="status"/>
        <result property="createUserId" column="create_user_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="pageQuery" resultMap="BaseResultMap">
        SELECT * FROM project
        <where>
            <if test="conditions.projectName != null and conditions.projectName != ''">
                AND project_name LIKE CONCAT('%', #{conditions.projectName}, '%')
            </if>
            <if test="conditions.status != null">
                AND status = #{conditions.status}
            </if>
            <!-- 可根据需要添加其他条件 -->
        </where>
        ORDER BY id DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM project
        <where>
            <if test="conditions.projectName != null and conditions.projectName != ''">
                AND project_name LIKE CONCAT('%', #{conditions.projectName}, '%')
            </if>
            <if test="conditions.status != null">
                AND status = #{conditions.status}
            </if>
        </where>
    </select>

</mapper>