<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.smarttestplatform.module.message.mapper.MessageMapper">

    <resultMap id="BaseResultMap" type="com.example.smarttestplatform.module.message.entity.Message">
        <id property="id" column="id"/>
        <result property="senderId" column="sender_id"/>
        <result property="receiverId" column="receiver_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="type" column="type"/>
        <result property="relatedId" column="related_id"/>
        <result property="relatedType" column="related_type"/>
        <result property="isRead" column="is_read"/>
        <result property="readTime" column="read_time"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="pageQuery" resultMap="BaseResultMap">
        SELECT * FROM message
        <where>
            <if test="conditions.receiverId != null">
                AND receiver_id = #{conditions.receiverId}
            </if>
            <if test="conditions.type != null and conditions.type != ''">
                AND type = #{conditions.type}
            </if>
            <if test="conditions.isRead != null">
                AND is_read = #{conditions.isRead}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM message
        <where>
            <if test="conditions.receiverId != null">
                AND receiver_id = #{conditions.receiverId}
            </if>
            <if test="conditions.type != null and conditions.type != ''">
                AND type = #{conditions.type}
            </if>
            <if test="conditions.isRead != null">
                AND is_read = #{conditions.isRead}
            </if>
        </where>
    </select>
    <update id="completeByRelated">
        UPDATE message SET status = 'completed'
        WHERE related_id = #{relatedId} AND related_type = #{relatedType} AND type = '待办' AND status = 'pending'
    </update>
</mapper>