<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.smarttestplatform.module.autotest.mapper.AutoTestCaseMapper">

    <resultMap id="BaseResultMap" type="com.example.smarttestplatform.module.autotest.entity.AutoTestCase">
        <id property="id" column="id"/>
        <result property="projectId" column="project_id"/>
        <result property="caseName" column="case_name"/>
        <result property="description" column="description"/>
        <result property="url" column="url"/>
        <result property="browser" column="browser"/>
        <result property="status" column="status"/>
        <result property="createUserId" column="create_user_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="pageQuery" resultMap="BaseResultMap">
        SELECT * FROM auto_test_case
        <where>
            <if test="conditions.projectId != null">
                AND project_id = #{conditions.projectId}
            </if>
            <if test="conditions.caseName != null and conditions.caseName != ''">
                AND case_name LIKE CONCAT('%', #{conditions.caseName}, '%')
            </if>
            <if test="conditions.status != null">
                AND status = #{conditions.status}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM auto_test_case
        <where>
            <if test="conditions.projectId != null">
                AND project_id = #{conditions.projectId}
            </if>
            <if test="conditions.caseName != null and conditions.caseName != ''">
                AND case_name LIKE CONCAT('%', #{conditions.caseName}, '%')
            </if>
            <if test="conditions.status != null">
                AND status = #{conditions.status}
            </if>
        </where>
    </select>

</mapper>